# nestMates

## Agent skills

### Issue tracker

需求和任务使用本地 Markdown 管理。创建、读取或更新工单前，阅读 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分诊标签。执行分诊或更新分诊状态前，阅读 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局：根目录 `CONTEXT.md` 与 `docs/adr/`。探索代码或提出领域、架构变更前，阅读 `docs/agents/domain.md`。
