# 原型验证记录

验证日期：2026-09-08。范围仅为静态粗原型，所有业务数据与写操作均为内存模拟。

## 浏览器行为检查

使用 Playwright CLI 驱动真实 Chromium，按按钮、输入框和页面结果核对：

| 路径 | 结果 |
| --- | --- |
| 概况七项指标、今日与近 7 日切换 | 通过 |
| 填写原因、停用确认、成功状态与恢复入口 | 通过 |
| 恢复明确失败、返回保留原因、重试成功 | 通过 |
| 保存编辑保留当前发布内容，明确发布后更新 | 通过 |
| 未保存编辑离开：取消保留输入、确认关闭弹窗并切页 | 通过（修复后） |
| 下架模板重新发布最后内容，保留另一价格的编辑稿 | 通过 |
| 缺项草稿可保存，发布校验阻止 | 通过 |
| 最后一名有效超级管理员不能降级 | 通过 |
| 运营管理员隐藏家庭入口、直接访问拒绝、日志仅本人 | 通过 |
| 提交前当前账号被停用，旧页面操作不生效 | 通过 |
| 恢复前成员退出归档，恢复拒绝、无重试恢复入口 | 通过 |
| 无匹配结果、读取失败后重新读取 | 通过 |
| 结构化周期控件切换、非法间隔拦截、合法间隔预览 | 通过 |
| 月周期半填草稿保存及发布失败后保留月数 | 通过（修复后） |

检查分段完成：综合脚本前 11 项断言通过，随后因导航与面包屑同名导致脚本定位冲突；剩余路径使用明确页面定位补验。一次脚本仅变 URL 片段未重置模拟身份，已改为完整重载；这些脚本问题不作为产品失败记录。上述只记录已实际执行且断言通过的路径，不将未执行步骤视为通过。

## 静态检查与审阅

- 内联 JavaScript 语法检查通过。
- 本轮工单与资产的相对链接核对通过；验证时工单保持 claimed；后续用户明确确认 Q4～Q6 后才记录 Answer 并结案。
- `git diff --check` 通过，但当前项目文件均未跟踪，因此它不覆盖新增资产；新增文件另外核对语法、链接与尾随空白。
- Impeccable 检测器仅运行一次，因 HTML/CSS 解析依赖不可用而降级为正则，未报告命中；这不能证明计算样式或对比度通过。
- 使用现有 quality_reviewer 角色替代本环境未提供的专用 Impeccable 完稿审阅角色。只读审阅发现并复核离开编辑页弹窗问题、月周期半填数据保留问题；主代理已修复并补跑相关浏览器路径。

## 证据与限制

最终截图已补齐：

- [桌面运行概况](../../../../output/playwright/admin-desktop-overview.png)、[手机运行概况](../../../../output/playwright/admin-mobile-overview.png)。
- [桌面模板对照](../../../../output/playwright/admin-desktop-editor.png)、[手机模板对照](../../../../output/playwright/admin-mobile-editor.png)。
- [家庭详情](../../../../output/playwright/admin-desktop-family.png)、[恢复确认](../../../../output/playwright/admin-desktop-confirm.png)、[权限不足](../../../../output/playwright/admin-desktop-forbidden.png)。

桌面视口为 1440×1000，手机为 390×844；所测页面的 documentElement.scrollWidth 均等于视口宽度，没有页面级横向溢出。主代理检查最终概况、模板及恢复确认截图，只读审阅代理检查全部七张；未见实质遮挡或裁切。原有有界面浏览器后续截图超时，未继续重复该路径，改用独立无界面 Chromium 完成最终捕获。

最终只读复核结论为 **Ship，可交用户试走**：前述修复项无剩余阻塞，截图通过本轮粗原型范围的检查。该结论仅覆盖修复项和所交截图，不扩展为正式产品、接口或权限实现验收。

- 无真实服务端鉴权、持久化、并发、网络结果未知处理或家庭端页面测试。
- 统计数字为模拟汇总，不是生产运营数据；没有制作完整首批模板内容库。
- 用户随后回复“都合适”，已确认 Q4～Q6 的页面路径与提示。该确认与模拟检查共同构成原型工单结案依据，不等于正式产品验收。
